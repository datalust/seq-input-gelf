version: '3'
services:
  sqelf:
    image: sqelf-ci:latest
    depends_on:
      - seq
    ports:
      - "12202:12201/udp"
    environment:
      SEQ_ADDRESS: "http://seq:5341"
    restart: unless-stopped
  seq:
    image: datalust/seq:latest
    ports:
      - "5342:80"
    environment:
      ACCEPT_EULA: Y
    restart: unless-stopped
    volumes:
      - ./seq-data:/data